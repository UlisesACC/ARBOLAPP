<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/animejs@3.2.1/lib/anime.min.js"></script>
    <title>Formulario de Árboles</title>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col justify-between">

    <!-- Barra de navegación -->
    <nav class="bg-white shadow-md">
        <div class="container mx-auto px-4 py-4 flex items-center justify-between">
            <div class="text-2xl font-bold text-green-700" id="logo">🎄 Arbol App 🎄</div>
            <ul class="flex space-x-6 text-gray-600 font-medium">
                <li><a href="/" class="hover:text-green-600">Inicio</a></li>
            </ul>
        </div>
    </nav>

    <!-- Contenedor del formulario -->
    <main class="flex-grow container mx-auto px-4 py-10">
        <div class="bg-white p-8 rounded-xl shadow-lg">
            <h1 id="titulo" class="text-3xl font-bold mb-4 text-center text-green-700">🎄 Formulario de Árboles 🎄</h1>
            <p id="subtitulo" class="text-gray-600 mb-8 text-center">Aquí podrás registrar un nuevo árbol.</p>

            <fieldset id="fieldset" class="border-2 border-green-300 rounded-lg p-6">
                <legend class="px-2 font-semibold text-green-700 text-lg">🌱 Agregando un árbol 🌱</legend>

                <form method="POST" action="/registrar_arbol" enctype="multipart/form-data" class="space-y-6 mt-6">
                    
                    <!-- Identificador del árbol -->
                    <div>
                        <label class="block text-gray-700 mb-2">Proporciona el número del árbol:</label>
                        <input type="number" name="numero_arbol" required class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-green-400">
                    </div>

                    <!-- Selección de especie -->
                    <div>
                        <label class="block text-gray-700 mb-2">Selecciona la especie:</label>
                        <select id="especie" name="especie" onchange="cargarSubespecies()" required class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-green-400">
                            <option value="">-- Selecciona una especie --</option>
                            <% especies.forEach(especie => { %>
                                <option value="<%= especie.id_especie %>">
                                    <%= especie.nombre %>
                                </option>
                            <% }) %>
                        </select>
                    </div>

                    <!-- Selección de subespecie -->
                    <div>
                        <label class="block text-gray-700 mb-2">Selecciona la subespecie:</label>
                        <select id="subespecie" name="subespecie" required class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-green-400">
                            <option value="">-- Selecciona una subespecie --</option>
                        </select>
                    </div>

                    <!-- Botón enviar -->
                    <div class="text-center">
                        <button type="submit" class="bg-green-500 text-white font-semibold px-6 py-2 rounded-full hover:bg-green-600 transition">
                            Registrar Árbol
                        </button>
                    </div>

                </form>
            </fieldset>
        </div>
    </main>

    <!-- Pie de página -->
    <footer class="bg-white shadow-inner py-4 mt-8">
        <div class="container mx-auto px-4 text-center">
            <p class="text-gray-700 mb-2">Consulta el siguiente PDF:</p>
            <a href="https://bioteca.biodiversidad.gob.mx/janium/Documentos/1480.pdf" target="_blank" class="text-green-600 hover:underline font-semibold">
                Flora del Valle de México
            </a>
        </div>
    </footer>

    <!-- Scripts de animación -->
    <script>
        // Animaciones iniciales
        anime({
            targets: "#logo",
            translateY: [-50, 0],
            opacity: [0, 1],
            duration: 1200,
            easing: "easeOutElastic(1, .8)"
        });

        anime({
            targets: "#titulo",
            scale: [0.7, 1],
            opacity: [0, 1],
            delay: 300,
            duration: 1000,
            easing: "easeOutExpo"
        });

        anime({
            targets: "#subtitulo",
            translateX: [-30, 0],
            opacity: [0, 1],
            delay: 600,
            duration: 800,
            easing: "easeOutQuad"
        });

        anime({
            targets: "#fieldset",
            opacity: [0, 1],
            translateY: [50, 0],
            delay: 900,
            duration: 900,
            easing: "easeOutCubic"
        });

        // Cargar subespecies dinámicamente
        async function cargarSubespecies() {
            const especie_id = document.getElementById("especie").value;
            if (!especie_id) return;

            const respuesta = await fetch(`/subespecies/${especie_id}`);
            const subespecies = await respuesta.json();

            const subespecieSelect = document.getElementById("subespecie");
            subespecieSelect.innerHTML = '<option value="">-- Selecciona una subespecie --</option>';

            subespecies.forEach(sub => {
                const option = document.createElement("option");
                option.value = sub.id_subespecie;
                option.textContent = sub.nombre;
                subespecieSelect.appendChild(option);
            });

            anime({
                targets: "#subespecie",
                scale: [0.9, 1],
                duration: 800,
                easing: "easeOutElastic(1, .6)"
            });
        }
    </script>
</body>
</html>
